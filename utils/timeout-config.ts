export const TIMEOUTS = {
    GLOBAL: 30_000, // For overall/global timeouts
    STEP: 10_000,   // For individual step timeouts
    CUSTOM: {
        ACCEPT_ALL: 5_000,
        CALENDAR_MONTH_LOOP: 24,
        CALENDAR_NEXT_WAIT: 200,
        // Add more custom timeouts as needed
    }
};